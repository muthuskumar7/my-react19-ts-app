const config = {
  semi: true,
  singleQuote: true,
  trailingComma: 'all',
  bracketSpacing: true,
  tabWidth: 2,
  useTabs: false,
  printWidth: 100,
  plugins: ['prettier-plugin-tailwindcss'],
};

export default config;
